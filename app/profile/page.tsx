'use client';
import { supabase } from '#/libs/SupabaseClient';
import React from 'react';

function page() {
  return (
    <main>
      <button>SignUp</button>
    </main>
  );
}

export default page;
